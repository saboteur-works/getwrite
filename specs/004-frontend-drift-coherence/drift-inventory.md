# Drift Inventory

This artifact captures frontend drift findings, risk ranking, invariant impact, and domain coverage for spec 004.

## Drift Taxonomy

Use one or more drift types per finding.

| Drift Type             | Definition                                                       | Typical Signal                                                              |
| ---------------------- | ---------------------------------------------------------------- | --------------------------------------------------------------------------- |
| size                   | A file/module has grown beyond easy review and change safety.    | High line count, broad API surface, difficult navigation.                   |
| responsibility         | A unit mixes multiple responsibilities that should be separated. | Data orchestration, rendering, and side effects combined in one unit.       |
| architectural-boundary | Layer boundaries are crossed or blurred.                         | UI components directly owning domain/state concerns meant for other layers. |
| duplication            | Similar logic or behavior is repeated across files.              | Copy/pasted handlers, schema logic, or menu/action definitions.             |
| standards              | Repository standards are inconsistently applied.                 | Type-safety erosion, token bypasses, mixed error-handling patterns.         |

## Risk Rubric

Risk level must be assigned to every finding.

| Risk   | Criteria                                                                           | Expected Action                                                       |
| ------ | ---------------------------------------------------------------------------------- | --------------------------------------------------------------------- |
| High   | Invariant impact is not none OR cross-domain blast radius is present.              | Prioritize first in execution blueprint and attach strict guardrails. |
| Medium | Localized structural drift with moderate maintenance cost and no invariant impact. | Schedule after High findings; define seam and verification scope.     |
| Low    | Cosmetic/isolated drift with low behavioral or coordination risk.                  | Track for opportunistic cleanup; avoid blocking critical sequence.    |

## Invariant-Impact Legend

Use these values for consistent impact tagging.

| Invariant Impact    | Meaning                                                                       |
| ------------------- | ----------------------------------------------------------------------------- |
| workspace           | Could alter protected Workspace position/behavior semantics.                  |
| revisions+canonical | Could alter revision linearity or canonical revision guarantees.              |
| resource-identity   | Could break identity-stable associations across move/rename flows.            |
| compile             | Could alter compile ordering or export-only non-mutation behavior.            |
| token-system        | Could bypass token-first styling or introduce inconsistent visual primitives. |
| none                | No direct invariant impact identified.                                        |

## Findings Register

Lower rank number means higher implementation priority.

| ID     | Rank | Domain                  | Target Area                                                                                                                                                                 | Drift Types                                         | Risk   | Invariant Impact    | Evidence Summary                                                                                                                                                                                                                                                                                                                                                                                              | Recommended Direction                                                                                                                                                                                        |
| ------ | ---- | ----------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------- | ------ | ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| DF-007 | 1    | Redux slices            | `frontend/src/store/revisionsSlice.ts`, `frontend/src/store/projectsSlice.ts`, `frontend/src/store/resourcesSlice.ts`                                                       | size, responsibility, architectural-boundary        | High   | revisions+canonical | `revisionsSlice.ts` is 884 lines and combines async transport (`fetch` thunks), normalization, revision selection, canonical state transitions, and UI-facing status flags. `projectsSlice.ts` and `resourcesSlice.ts` duplicate projection and mutation concerns, with normalization/cache logic and side-effectful reorder persistence in slice-adjacent thunks.                                            | Move transport and normalization into dedicated services/selectors, keep slices focused on state transitions, and isolate canonical-revision invariants behind explicit reducer-level guard helpers.         |
| DF-002 | 2    | work area/editor        | `frontend/components/WorkArea/EditView.tsx`                                                                                                                                 | responsibility, architectural-boundary, size        | High   | revisions+canonical | `EditView` (637 lines) mixes editor rendering, revision/canonical fetch + persistence (`/api/resource/revision/*`, `/api/project-resources`), autosave state machine, retry logic, and appearance preference listeners. Revision-sensitive persistence is embedded in UI component flow, increasing regression risk around canonical guarantees.                                                              | Extract revision I/O and autosave orchestration into dedicated hooks/services (`useRevisionContent`, `useCanonicalAutosave`) and keep `EditView` focused on presentational/editor interactions.              |
| DF-003 | 3    | resource tree           | `frontend/components/ResourceTree/ResourceTree.tsx`                                                                                                                         | responsibility, architectural-boundary, size        | High   | resource-identity   | `ResourceTree` (600 lines) owns resource-to-tree normalization, custom selection semantics, drag-drop reparent/reorder handling, and persistence dispatch (`updateFolders`, `updateResources`, `persistReorder`). Data-shape adaptation and ordering side effects are tightly coupled to render logic.                                                                                                        | Introduce a tree data adapter plus reorder command layer so drag/drop semantics and identity/order persistence can be validated independently of tree rendering.                                             |
| DF-008 | 4    | project types manager   | `frontend/components/project-types/ProjectTypesManagerPage.tsx`                                                                                                             | size, responsibility, standards                     | High   | workspace           | `ProjectTypesManagerPage` (838 lines) manages theme resolution, list navigation, folder/resource editing forms, and modal/page branching in one component. Because this surface edits project-type defaults (including Workspace-seed structure), drift here can propagate invalid templates into future project creation flows.                                                                              | Extract manager into smaller units (`ProjectTypeListPane`, `ProjectTypeEditorForm`, `ProjectTypeDraftService`) and add explicit Workspace-folder guardrails at update boundaries.                            |
| DF-006 | 5    | model utilities         | `frontend/src/lib/models/resource.ts`, `frontend/src/lib/models/project-view.ts`, `frontend/src/lib/models/resource-templates.ts`                                           | responsibility, standards, architectural-boundary   | High   | resource-identity   | Model utility layer mixes schema/runtime validation, filesystem side effects, adapter reshaping for legacy UI, and template management in the same hotspots. Evidence includes broad `resource.ts` persistence + creation responsibilities, adapter casts in `project-view.ts` (`(r as any)` / metadata order handling), and template scanners in `resource-templates.ts` using permissive casts.             | Split model utilities by seam (`resource-factory`, `resource-persistence`, `project-view-adapter`, `template-service`) and replace permissive casts with shared typed helpers for metadata/order extraction. |
| DF-001 | 6    | app shell               | `frontend/components/Layout/AppShell.tsx`                                                                                                                                   | size, responsibility, architectural-boundary        | High   | none                | `AppShell` is 1486 lines and combines layout resizing, global settings UX, command palette state, project-type loading via `/api/project-types`, editor-save coordination, and multiple modal orchestration paths (`ContextAction`, `CreateModal`, `ExportModal`, `CompileModal`). This creates cross-domain blast radius and hard-to-isolate changes.                                                        | Split shell into focused orchestrators: `ShellLayoutController`, `ShellSettingsMenu`, `ShellModalCoordinator`, and `ShellProjectTypeLoader`, with explicit behavior contracts per seam.                      |
| DF-004 | 7    | modal flows             | `frontend/components/Start/ManageProjectMenu.tsx`, `frontend/components/common/CompilePreviewModal.tsx`, `frontend/components/common/ConfirmDialog.tsx`                     | responsibility, duplication, architectural-boundary | Medium | compile             | Modal behavior is fragmented across components: `ManageProjectMenu` mixes UI state with direct API calls (`/api/project/delete`, `/api/project/rename`) while also delegating callbacks; `CompilePreviewModal` mixes scope selection, descendant traversal, preview generation, and legacy compatibility behavior in one unit. This creates inconsistent orchestration and duplicated close/confirm patterns. | Centralize modal flow orchestration into a project actions controller; keep modal components stateless where possible and move API/callback branching into one coordination seam.                            |
| DF-010 | 8    | editor menu duplication | `frontend/components/Editor/MenuBar/MenuBar.tsx`, `frontend/components/Editor/MenuBar/EditorMenuInput.tsx`, `frontend/components/Editor/MenuBar/EditorMenuColorSubmenu.tsx` | duplication, responsibility, standards              | Medium | none                | Menu command wiring is repeated across many inline `EditorMenuIcon` callbacks in `MenuBar.tsx`; color/highlight controls repeat nearly identical submenu wiring three times; input variants in `EditorMenuInput.tsx` duplicate state/change scaffolding per type. This raises maintenance cost and increases behavior drift between menu actions.                                                             | Convert command groups to declarative config maps and centralize shared command/input wiring so icon, tooltip, active-state, and handler composition are generated from one schema.                          |
| DF-005 | 9    | sidebar metadata        | `frontend/components/Sidebar/MetadataSidebar.tsx`                                                                                                                           | standards, responsibility, duplication              | Medium | none                | Sidebar metadata logic duplicates folder-list derivation three times (characters/locations/items), mirrors selected resource metadata into local state with multiple `as any` casts, and contains unused sample fallback (`sampleCharacters`). This weakens type safety and increases update drift risk across metadata controls.                                                                             | Replace repeated selectors with typed shared selectors/hooks (`useMetadataOptions`) and reduce local mirrored state to a minimal controlled-form model with typed metadata accessors.                        |
| DF-009 | 10   | help surface            | `frontend/components/help/HelpPage.tsx`                                                                                                                                     | size, responsibility, duplication                   | Low    | none                | `HelpPage` (532 lines) co-locates static copy, tab definitions, presentational primitives, and modal-shell behavior in one file. Large inline content blocks and repeated section-card patterns make updates brittle and increase drift between docs intent and UI presentation.                                                                                                                              | Separate structured help content data from rendering components and keep modal/page wrapper logic in a thin shell component.                                                                                 |

## Domain Coverage

Every in-scope domain must be marked as either finding-recorded or no-material-drift.

| Domain                  | Coverage Status  | Linked Finding IDs | Notes                                                                                           |
| ----------------------- | ---------------- | ------------------ | ----------------------------------------------------------------------------------------------- |
| app shell               | finding-recorded | DF-001             | Large multi-responsibility shell with cross-domain orchestration and high blast radius.         |
| work area/editor        | finding-recorded | DF-002             | Edit surface couples UI with canonical revision persistence and autosave control flow.          |
| resource tree           | finding-recorded | DF-003             | Tree rendering and reorder persistence are tightly coupled in one hotspot component.            |
| modal flows             | finding-recorded | DF-004             | Modal intent handling is split across menu + modal components with mixed side-effect ownership. |
| sidebar metadata        | finding-recorded | DF-005             | Metadata control surface has repeated selectors and weak typing patterns.                       |
| model utilities         | finding-recorded | DF-006             | Model helpers blur factory, persistence, and adapter responsibilities with permissive casts.    |
| Redux slices            | finding-recorded | DF-007             | Slice boundaries mix transport and invariant-sensitive revision orchestration.                  |
| project types manager   | finding-recorded | DF-008             | Large manager surface combines list/editor orchestration and template-authoring concerns.       |
| help surface            | finding-recorded | DF-009             | Help content and help UI shell are tightly coupled in one oversized page component.             |
| editor menu duplication | finding-recorded | DF-010             | Editor toolbar action wiring is repetitive and prone to drift across controls.                  |

No in-scope domain is currently marked `no-material-drift` in this pass; all 10 domains have at least one material hotspot finding recorded.
