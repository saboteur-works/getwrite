# Tasks: Entity graph node dragging

Source spec: `specs/features/entity-graph-node-dragging.md`. Granularity: story points (1/2/3/5/8).

### Task 1: Add live position override state and the click/drag threshold constant
**What:** Adds a `nodePositionOverrides` state value to `EntityGraphCanvas.tsx` — a `Map<string, { x: number; y: number }>` keyed by `entityId`, initialized once via `useState(() => new Map())` — held as plain component state outside `computeGraphLayout`'s `useMemo` so it is never reset when `nodes`/`edges`/`width`/`height` change (FR-2), mirroring this component's own existing `selectedNodeId`/`pan`/`scale` precedent exactly (resolved OQ-2: no purge, no reset, no reverse lookup). Also adds a single named `DRAG_CLICK_THRESHOLD_PX` constant near the file's other tuning constants (`MIN_SCALE`, `ZOOM_STEP_FACTOR`, etc.), with a comment recording its value as an unverified starting point tuned during manual verification (Task 7), not asserted as correct (FR-7, OQ-1). Neither is wired into rendering or event handling yet — this task is scaffolding only, so Tasks 2-4 each land on a stable foundation rather than each re-deriving it.
**Files:** frontend/components/WorkArea/Views/EntityRelationshipGraphView/EntityGraphCanvas.tsx
**Done when:** `nodePositionOverrides` state exists, typed `Map<string, { x: number; y: number }>`, declared outside the `computeGraphLayout` `useMemo` and its dependency array; `DRAG_CLICK_THRESHOLD_PX` exists as a single named constant (not inlined anywhere) with an adjacent comment stating its value is an unverified starting point; `computeGraphLayout` itself has zero diff — same signature, same pure fixed-300-tick contract; `pnpm --filter getwrite-frontend typecheck` passes.
**Depends on:** none
**Estimate:** 2
**Notes:** Deliberately inert — nothing observable changes yet. This keeps Tasks 2 and 3 (the highest-risk work, per the spec) from having to also invent the state shape under pressure.
**Done:** [x]

### Task 2: Wire node drag mechanics — reposition the dragged node only
**What:** Adds a mouse-down handler to each node's `<g>` (in addition to its existing `onClick`/`onKeyDown`) that records the gesture's start client coordinates and the node's current resolved position (override if one exists, else `computeGraphLayout`'s `x`/`y`) into a ref — mirroring the existing `dragOriginRef`/`handleBackgroundMouseDown` pattern this file already uses for canvas panning, not a new event-handling approach. Extends the existing window-level `mousemove`/`mouseup` effect (the same one that already drives background panning) to also, while a node drag is in progress, convert the client-pixel delta since gesture start into a viewBox-unit delta using the exact same conversion the wheel handler already derives in this file — `svgRef.current.getBoundingClientRect()`, falling back to a 1:1 ratio when the element is zero-sized (jsdom), and dividing by the current `scale` — then writes the node's new resolved position into `nodePositionOverrides` (FR-1, FR-7). Renders each node's `<g transform="translate(...)">` from its resolved position (override if present, else the layout's own `x`/`y`) rather than directly from `computeGraphLayout`'s output, satisfying FR-2. No other node's position, and no `computeGraphLayout` input or output, changes during this gesture (FR-4) — dragging one node writes exactly one entry in the override map.
**Files:** frontend/components/WorkArea/Views/EntityRelationshipGraphView/EntityGraphCanvas.tsx, frontend/tests/component/EntityGraphCanvas.test.tsx
**Done when:** a test simulates a mouse-down on one node's `<g>` followed by a window-level `mousemove` past `DRAG_CLICK_THRESHOLD_PX` and confirms that node's rendered `transform` translate coordinates change to follow the pointer, converted through the scale-aware ratio (asserted at `scale !== 1` as well as the default, mirroring the existing wheel-anchor test's own two-case pattern); a test confirms every *other* rendered node's `transform` is unchanged by that same gesture (FR-4); a test confirms a `mouseup` after the drag leaves the moved node's new position in place (no snap-back); a test confirms none of `forceSimulation`/`.tick`/`.restart`/`.alphaTarget` is invoked during the gesture (spy on `d3-force`'s exports, confirming zero new calls beyond the one `computeGraphLayout` call already made on mount) — the concrete evidence backing FR-4's "does not drive `d3-force`" clause.
**Depends on:** 1
**Estimate:** 5
**Notes:** This task only makes the node follow the pointer. It deliberately does not yet stop the eventual `onClick`/`onNodeActivated` from also firing on release — that discrimination is Task 3, kept separate because the spec calls it the highest-risk item and it deserves its own focused tests rather than being buried inside this task's.
**Done:** [ ]

### Task 3: Click-vs-drag discrimination — suppress selection/navigation once the threshold is crossed
**What:** Tracks, in the same ref-based gesture state Task 2 introduced, whether the current gesture's client-pixel movement has exceeded `DRAG_CLICK_THRESHOLD_PX` at any point before release (compared in client pixels, before any viewBox-unit conversion, per FR-7 — a raw `Math.hypot`/max-axis distance between the mouse-down client coordinates and the current client coordinates, never the already-scaled viewBox delta Task 2 computes for repositioning). If the threshold was never exceeded by the time `mouseup` fires, the gesture is left to behave exactly as today's plain click does — `handleNodeActivate` runs unmodified, toggling `selectedNodeId` and calling `onNodeActivated` (FR-5). If the threshold was exceeded at any point, the node's own `onClick` (which fires after `mouseup` for a native browser click) must be suppressed for that one gesture: neither `selectedNodeId` nor `onNodeActivated` may fire, regardless of where the pointer was released — on the dragged node, another node, or empty canvas (FR-6).
**Files:** frontend/components/WorkArea/Views/EntityRelationshipGraphView/EntityGraphCanvas.tsx, frontend/tests/component/EntityGraphCanvas.test.tsx
**Done when:** a test simulates mouse-down + mouse-move-under-threshold + mouse-up on a node and confirms `onNodeActivated` is called and `selectedNodeId` toggles, identical to today's plain-click test (FR-5 — no regression on the existing "toggles a selected-state visual attribute on exactly the clicked node" and "calls onNodeActivated" tests, which must still pass unmodified); a test simulates mouse-down + mouse-move-over-threshold + mouse-up on the same node and confirms `onNodeActivated` is NOT called and `selectedNodeId` does not toggle (FR-6); a test confirms the over-threshold case still holds when the mouse-up is dispatched over a *different* node's `<g>` or over the background `<rect>` rather than back over the original node (FR-6's "on or off that node" clause); a test confirms the threshold comparison itself is unaffected by the canvas's current `scale` — the identical client-pixel movement classifies the same way (drag or click) at `scale: 1` and at a non-default `scale`, evidencing the "compared in client pixels, before viewBox conversion" requirement of FR-7.
**Depends on:** 2
**Estimate:** 5
**Notes:** This is the spec's own highest-risk item — a missed case here means finishing a drag navigates the writer out of the graph. Do not conflate "distance travelled in viewBox units" with "distance travelled in client pixels" anywhere in this task's comparison; Task 2's delta (viewBox units, scale-divided) and this task's threshold check (raw client pixels) are two separate numbers computed from the same raw mouse coordinates, not one reused for both purposes.
**Done:** [ ]

### Task 4: Resolve edge endpoints from live position overrides at render time
**What:** Changes how each edge's rendered `x1`/`y1`/`x2`/`y2` are derived: rather than reading `positionedEdges`' baked-in coordinates directly (as today), the render loop resolves each endpoint from `edgeEndpoints(positioned.edge)`'s two entity ids through the same override-or-layout resolution Task 2 built for nodes — an endpoint whose entity has a live override renders at that override's position; an endpoint whose entity has none continues to render at `computeGraphLayout`'s original, unchanged coordinate (FR-3). **This changes a shape the existing test suite asserts on directly**: any existing test that reads a `PositionedEdge`'s `x1`/`y1`/`x2`/`y2` off `computeGraphLayout`'s return value and expects that same value to appear in the rendered `<line>` (e.g. "shares its visible sibling line's x1/y1/x2/y2 endpoints") must be reviewed and, where it now diverges only because a render-time resolution step was inserted with no override present (so values are unchanged in practice), left passing as-is — this task's tests exist specifically to catch the one case that does change: a dragged node's attached edges must show updated endpoints in the rendered `<line>`, while `computeGraphLayout`'s own return value for that same edge stays exactly what it was before the drag (proving the resolution happens at render time, not by mutating layout output).
**Files:** frontend/components/WorkArea/Views/EntityRelationshipGraphView/EntityGraphCanvas.tsx, frontend/tests/component/EntityGraphCanvas.test.tsx
**Done when:** a test drags a node past the threshold and confirms every edge whose `edgeEndpoints` includes that node's `entityId` re-renders with the matching `x1`/`y1` or `x2`/`y2` (whichever end that node is) equal to the node's new overridden position, both for the hit-target `<line>` and its visible sibling (Task 4/Task 5 of `entity-graph-edge-tooltips` established both must share endpoints, and that invariant must survive here too); a test confirms an edge's *other* endpoint — the one not attached to any dragged node — still renders at `computeGraphLayout`'s original coordinate, unchanged; a test confirms `computeGraphLayout`'s own return value for that edge (called directly, independent of the component) is identical before and after the drag, evidencing that resolution happens at render time and the pure function itself was never touched; the full existing `EntityGraphCanvas.test.tsx` suite is re-run and any assertion that broke purely from the added resolution indirection (not from an actual behavior regression) is updated to match, with the reason noted in the test file or PR description.
**Depends on:** 1, 2
**Estimate:** 3
**Notes:** Sequenced after Task 2 (which resolves node positions) rather than in parallel with it, since this task's resolution helper reuses the same override-or-layout lookup Task 2 introduces for a node — building it twice independently would risk the two falling out of sync on what "resolved position" means for a given entity.
**Done:** [ ]

### Task 5: Verify no simulation coupling, no persistence, no new flag, no entity-data access
**What:** A verification task (no new production behavior expected beyond what Tasks 1-4 already built) confirming the remaining constraints the spec states but that Tasks 1-4's own tests do not each individually re-assert: that a drag never restarts or re-ticks the `d3-force` simulation beyond Task 2's own spy-based test, that no drag path writes to `localStorage`, a sidecar, project config, or any API (FR-8), that no new per-project feature flag is checked anywhere in the touched code (FR-9, still gated solely on the existing `entities` flag one level up in `AppShell.tsx`), and that no drag path reads or calls into `lib/api/entity-cooccurrence.ts`, `lib/api/entity-relationships.ts`, `entityAliasTableSlice`, or any other entity-data source (FR-10).
**Files:** frontend/tests/component/EntityGraphCanvas.test.tsx (extension of the existing "never reads from or writes to localStorage for position data" / "never calls fetch for position data" tests to also drag a node before asserting), none (grep verification, no source changes expected)
**Done when:** the existing `localStorage`-spy and `fetch`-spy tests are extended to perform a full drag gesture (mouse-down, move past threshold, mouse-up) before their assertions, and both still pass with zero calls recorded (FR-8, FR-10's transport half); a grep across `EntityGraphCanvas.tsx`'s diff from Task 1 onward confirms no reference to `localStorage`, `sidecar`, `updateSidecar`, `entity-cooccurrence`, `entity-relationships`, `entityAliasTableSlice`, or any feature-flag name other than `entities`; a test confirms dragging a node does not change `positionedNodes`/`positionedEdges`' length or any node's `entityKind`/`name`/`aliases`-shaped data — only `x`/`y`-shaped position data moves.
**Depends on:** 4
**Estimate:** 2
**Notes:** Lightweight by design, matching the sibling edge-tooltips list's own Task 7 precedent — Tasks 2 and 4 already assert most of this in passing; this task is the explicit cross-check tying those assertions back to FR-4/FR-8/FR-9/FR-10 by name before sign-off, and extends two tests that would otherwise still only be exercising the pre-drag code path.
**Done:** [ ]

### Task 6: Storybook story for node dragging
**What:** Extends `EntityGraphCanvas.stories.tsx` with a story demonstrating the drag gesture, matching `docs/standards/storybook-implementation.md`'s conventions and this component's existing story structure (following the same pattern the edge-tooltips feature's own Task 6 used) rather than introducing a new stories file.
**Files:** frontend/stories/WorkArea/EntityGraphCanvas.stories.tsx
**Done when:** the new story renders without error in Storybook; the story's play function drags one fixture node a visible distance from its initial layout position and confirms (via `within(canvasElement)` / a `data-testid="entity-graph-node"` lookup) that node's rendered position differs from its pre-drag position while a second, undragged node's position is unchanged; the story's fixture includes at least one edge attached to the dragged node so the edge-follows-node behavior (Task 4) is visibly demonstrated in the same story.
**Depends on:** 4
**Estimate:** 2
**Notes:** Deliberately depends only on Task 4 (both drag mechanics and edge-following need to exist to demonstrate the full gesture) rather than on Task 5, since Task 5 adds no new rendering behavior for a story to exercise.
**Done:** [ ]

### Task 7: Manual verification pass, including tuning the click/drag threshold
**What:** Exercises the shipped drag gesture by hand in the running desktop/web app, confirming behavior the automated suite cannot fully assert by feel — most importantly, tuning `DRAG_CLICK_THRESHOLD_PX` against real trackpad/mouse usage, since its initial value (Task 1) is explicitly recorded as unverified rather than correct.
**Files:** specs/features/entity-graph-node-dragging/manual-verification.md (new — the artifact this task's results are written to; no source changes expected unless the threshold constant itself is adjusted as a result, in which case that one-line change lands alongside this file in the same task)
**Done when:** `manual-verification.md` records, for each of the following, what was actually observed (not merely "confirmed" with no detail): (1) a deliberate plain click on a node still toggles its selection ring and navigates away exactly as before this feature shipped; (2) a slow, small hand movement that the writer would call "just clicking, my hand wobbled a little" does not misfire as a drag — recorded as pass/fail against the threshold value actually shipped, with the value itself written down; (3) a deliberate drag of a reasonable hand-distance repositions the node and does NOT navigate away, confirmed by watching that the resource/view does not change after releasing; (4) an edge attached to the dragged node visibly follows it to the new position, while an edge to an undragged neighbour does not move; (5) a dragged node's position does not survive a page reload or a switch away from and back to the Graph view (FR-8, spot-checked by hand even though Task 5 already covers this by test); (6) if the threshold value shipped in Task 1 felt wrong during (2) or (3) above, the specific adjustment made and the new value, written to this same file and to the constant's in-code comment.
**Depends on:** 3, 4, 6
**Estimate:** 2
**Notes:** This is the one place in this task list where the threshold's actual number is allowed to be written down — every other task's text deliberately avoids asserting a specific pixel value, per the spec's own instruction that the starting value is unverified. If manual testing changes the constant, that is a one-line follow-up to Task 1's file, not a reason to reopen Tasks 2-4's logic.
**Done:** [ ]

## Summary
- Total tasks: 7
- Total estimated effort: 21 points
- Critical path: 1 -> 2 -> 3; 1, 2 -> 4; 4 -> 5; 4 -> 6; 3, 4, 6 -> 7 (linear-ish, per the spec's fully-resolved open questions — no spikes and no mutually exclusive branches anywhere in this list)
- Risks: Task 3 (click-vs-drag discrimination) is the single highest-risk task in this list — a missed release-target case would let a finished drag navigate the writer out of the graph, exactly the regression FR-6 exists to prevent. Task 4's endpoint-resolution change touches an existing test assertion shape directly (`PositionedEdge`'s baked-in coordinates vs. render-time resolution); reviewing rather than blindly updating any test that breaks is the safeguard against silently weakening that coverage. Task 7's threshold tuning is inherently a feel-based judgment call with no automated backstop, which is why its done-when requires a written record rather than a verbal "felt fine."

## Open Questions

None new. All three of the source spec's open questions (OQ-1: threshold value and unit, OQ-2: no purge of stale overrides, OQ-3: no bounds constraint) are already resolved in the spec itself, and no task in this list reopens or depends on re-deciding any of them — Task 1 records OQ-1's constant as unverified rather than asserting a value, Task 1's state design follows OQ-2's no-purge precedent directly, and no task anywhere clamps a dragged position to the viewBox per OQ-3.
