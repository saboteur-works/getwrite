name: CLI smoke tests

on:
    push:
        branches: [main, master]
    pull_request:
        branches: [main, master]

jobs:
    build-and-smoke:
        runs-on: ubuntu-latest
        strategy:
            fail-fast: false
            matrix:
                node-version: [20.x]
        steps:
            - name: Checkout repository
              uses: actions/checkout@v4

            - name: Setup pnpm
              uses: pnpm/action-setup@v2

            - name: Use Node.js
              uses: actions/setup-node@v4
              with:
                  node-version: 20

            - name: Setup Node.js
              uses: actions/setup-node@v4
              with:
                  node-version: ${{ matrix.node-version }}
                  cache: "pnpm"

            - name: Install dependencies
              run: pnpm install --frozen-lockfile

            - name: Typecheck CLI
              run: pnpm --filter getwrite-cli typecheck

            - name: CLI unit tests
              run: pnpm --filter getwrite-cli test

            - name: Build CLI artifacts
              run: pnpm --filter getwrite-cli build

            - name: Smoke test - getwrite-cli help
              run: node cli/dist/bin/getwrite-cli.cjs --help | head -n 20

            - name: Smoke test - getwrite-cli prune help
              run: node cli/dist/bin/getwrite-cli.cjs prune --help | head -n 20

            - name: Smoke test - getwrite-cli doctor help
              run: node cli/dist/bin/getwrite-cli.cjs doctor --help | head -n 20
